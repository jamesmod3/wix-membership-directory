# Testing

* install the form widget: create a member only page and put the widget on there
    - if member has proper subscription, they can submit
    - subqequent submissions are idempotent - a single member can only submit a single form record via upsert
* install the repeater widget: you just connect it to the correct dataset
    - ensure that the fields match
* figure out the dynamic pages thing
